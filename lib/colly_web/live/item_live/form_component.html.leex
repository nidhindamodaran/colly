<%= f = form_for @changeset, "#",
  id: "item-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= textarea f, :content %>
  <%= error_tag f, :content %>


  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>